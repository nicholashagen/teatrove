<% template compare(Object test1, Object test2)

/*
start = currentDate()
end = currentDate()
'A: ' (start > end ? 'true' : 'false') '\n'
*/

/*
c = test1 <=> tes2
'C: ' c '\n'
*/

getContext().doSomething('test', 5.2, 2.3, 2.9, 'test') 
'\n'
